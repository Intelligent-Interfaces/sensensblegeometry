name: Paper Compilation & Lean Formalization CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'research/**'
      - 'proofs/**'
      - '.github/workflows/paper-and-lean.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'research/**'
      - 'proofs/**'
      - '.github/workflows/paper-and-lean.yml'

jobs:
  paper:
    name: Compile & Publish LaTeX Papers
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compile Platform Architecture Paper
        uses: xu-cheng/latex-action@v3
        with:
          root_file: sensensble_geometry_platform.tex
          working_directory: research

      - name: Compile Basis Expansion Paper
        uses: xu-cheng/latex-action@v3
        with:
          root_file: basis_expansion_duality.tex
          working_directory: research

      - name: Upload Platform Paper Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Sensensble-Geometry-Platform-Paper
          path: research/sensensble_geometry_platform.pdf

      - name: Upload Duality Paper Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Basis-Expansion-Duality-Paper
          path: research/basis_expansion_duality.pdf

  lean:
    name: Verify Lean 4 Clifford Algebra Proofs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Elan (Lean version manager)
        run: |
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh -s -- -y --default-toolchain leanprover/lean4:stable
          echo "$HOME/.elan/bin" >> $GITHUB_PATH

      - name: Lean 4 Environment Info
        run: lean --version

      # When proofs/ lakefile.lean is present:
      # - name: Build & Verify Lean Proofs
      #   run: cd proofs && lake build
